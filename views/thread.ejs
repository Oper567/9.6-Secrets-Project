<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apugo | <%= thread.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background: radial-gradient(circle at top right, #020617, #0f172a); color: #e2e8f0; font-family: 'Plus Jakarta Sans', sans-serif; }
        .glass-card { background: rgba(30, 41, 59, 0.3); border: 1px solid rgba(255, 255, 255, 0.05); backdrop-filter: blur(12px); }
    </style>
</head>
<body class="min-h-screen pb-20">
    <nav class="p-6">
        <a href="/forum" class="text-blue-500 font-black text-[10px] uppercase tracking-widest hover:text-white transition-colors">
            <i class="fas fa-arrow-left mr-2"></i> Return to Hall
        </a>
    </nav>

    <main class="max-w-4xl mx-auto px-4">
        <div class="glass-card rounded-[2.5rem] p-8 md:p-12 mb-8">
            <div class="flex items-center gap-4 mb-8">
                <div class="w-12 h-12 rounded-xl bg-blue-600 flex items-center justify-center font-bold text-white overflow-hidden">
                    <% if (thread.author_pic) { %>
                        <img src="<%= thread.author_pic %>" class="w-full h-full object-cover">
                    <% } else { %>
                        <%= (thread.author_email || 'V')[0].toUpperCase() %>
                    <% } %>
                </div>
                <div>
                    <p class="text-[10px] font-black text-blue-500 uppercase tracking-widest">Discussion by <%= thread.author_email.split('@')[0] %></p>
                    <p class="text-slate-500 text-[10px] uppercase"><%= new Date(thread.created_at).toLocaleDateString() %></p>
                </div>
            </div>

            <h1 class="text-3xl md:text-4xl font-black text-white mb-6 leading-tight"><%= thread.title %></h1>
            <p class="text-slate-300 text-lg leading-relaxed mb-8 whitespace-pre-wrap"><%= thread.content %></p>

            <% if (thread.media_url) { %>
                <div class="rounded-3xl overflow-hidden border border-white/5 mb-8">
                    <img src="<%= thread.media_url %>" class="w-full object-cover max-h-[500px]" alt="Discussion Media">
                </div>
            <% } %>

            <div class="flex items-center gap-6 pt-8 border-t border-white/5">
                <button class="text-slate-400 hover:text-blue-500 transition-colors flex items-center gap-2">
                    <i class="far fa-heart"></i> <span class="text-xs font-black uppercase">Support</span>
                </button>
                <button class="text-slate-400 hover:text-blue-500 transition-colors flex items-center gap-2">
                    <i class="far fa-comment"></i> <span class="text-xs font-black uppercase">Reply</span>
                </button>
            </div>
        </div>

        <div class="space-y-4">
            <h3 class="text-[10px] font-black text-slate-500 uppercase tracking-[0.3em] mb-6">Village Responses</h3>
            <div class="glass-card rounded-3xl p-6 text-center italic text-slate-500 text-sm">
                The elders are currently silent... be the first to reply.
            </div>
        </div>
    </main>
</body>
</html>