<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, viewport-fit=cover">
    <title>Apugo | Village Directory</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">

    <style>
        body { 
            background: radial-gradient(circle at top right, #0f172a, #020617); 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            color: #e2e8f0; 
        }
        .glass-nav {
            background: rgba(2, 6, 23, 0.8);
            backdrop-filter: blur(24px);
            border-bottom: 1px solid rgba(255,255,255,0.03);
        }
        .villager-card {
            background: rgba(15, 23, 42, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .villager-card:hover {
            transform: translateY(-5px);
            border-color: rgba(59, 130, 246, 0.4);
            background: rgba(15, 23, 42, 0.7);
            box-shadow: 0 20px 40px -10px rgba(0,0,0,0.5);
        }
    </style>
</head>
<body class="min-h-screen pb-20">

    <nav class="glass-nav sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
            <div class="flex items-center gap-6">
                <h1 class="text-2xl font-black text-blue-500 italic tracking-tighter cursor-pointer" onclick="window.location.href='/feed'">
                    APUGO
                </h1>
                <p class="hidden md:block text-[10px] font-black uppercase tracking-[0.3em] text-slate-500 border-l border-white/10 pl-6">Village Directory</p>
            </div>
            <a href="/feed" class="text-[10px] font-black uppercase tracking-widest text-slate-400 hover:text-white transition-all">
                <i class="fas fa-chevron-left mr-2"></i> Back to Square
            </a>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-6 mt-12">
        
        <div class="flex flex-col md:flex-row md:items-end justify-between gap-8 mb-12">
            <div>
                <div class="flex items-center gap-3 mb-2">
                    <div class="h-[2px] w-8 bg-blue-600"></div>
                    <p class="text-[10px] font-black text-blue-500 uppercase tracking-[0.4em]">The Population</p>
                </div>
                <h2 class="text-4xl font-extrabold text-white tracking-tight">Meet your <span class="text-blue-500">Neighbors.</span></h2>
            </div>

            <div class="w-full md:w-96 relative group">
                <form action="/villagers" method="GET">
                    <input type="text" name="search" placeholder="Search by name or email..." value="<%= search %>"
                        class="w-full bg-white/5 border border-white/10 rounded-2xl py-4 px-12 text-[11px] font-bold uppercase tracking-widest outline-none focus:border-blue-500/50 transition-all text-white placeholder-slate-600">
                    <i class="fas fa-search absolute left-5 top-1/2 -translate-y-1/2 text-slate-600 text-xs group-focus-within:text-blue-500"></i>
                </form>
            </div>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            <% if (villagers && villagers.length > 0) { %>
                <% villagers.forEach(v => { %>
                    <div class="villager-card rounded-[2.5rem] p-8 text-center relative overflow-hidden group">
                        <div class="absolute -top-10 -right-10 w-32 h-32 bg-blue-600/5 rounded-full blur-3xl group-hover:bg-blue-600/10 transition-all"></div>
                        
                        <div class="relative z-10">
                            <div class="w-20 h-20 mx-auto mb-6 rounded-[2rem] overflow-hidden bg-slate-800 ring-4 ring-white/5 group-hover:ring-blue-500/20 transition-all">
                                <% if (v.profile_pic) { %>
                                    <img src="<%= v.profile_pic %>" class="w-full h-full object-cover">
                                <% } else { %>
                                    <div class="w-full h-full flex items-center justify-center font-black text-blue-500 text-2xl uppercase">
                                        <%= v.email[0] %>
                                    </div>
                                <% } %>
                            </div>

                            <h3 class="text-lg font-bold text-white mb-1 flex items-center justify-center gap-2">
                                @<%= v.email.split('@')[0] %>
                                <% if (v.is_verified) { %>
                                    <i class="fas fa-certificate text-blue-500 text-[10px]"></i>
                                <% } %>
                            </h3>
                            <p class="text-[9px] font-black uppercase tracking-[0.2em] text-slate-500 mb-8">Resident Villager</p>

                            <div class="flex flex-col gap-2">
                                <form action="/friends/request/<%= v.id %>" method="POST">
                                    <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white py-3 rounded-xl text-[10px] font-black uppercase tracking-widest transition-all active:scale-95">
                                        Send Request
                                    </button>
                                </form>
                                <a href="/messages?userId=<%= v.id %>" class="w-full bg-white/5 hover:bg-white/10 text-slate-300 py-3 rounded-xl text-[10px] font-black uppercase tracking-widest transition-all">
                                    Message
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full py-32 text-center">
                    <div class="w-20 h-20 bg-slate-900/50 rounded-full flex items-center justify-center mx-auto mb-6 border border-white/5">
                        <i class="fas fa-user-slash text-slate-700 text-2xl"></i>
                    </div>
                    <p class="text-slate-500 font-black uppercase tracking-[0.3em] text-xs">No neighbors found in this sector</p>
                    <a href="/villagers" class="text-blue-500 text-[10px] font-black uppercase tracking-widest mt-4 inline-block hover:underline">Clear Search</a>
                </div>
            <% } %>
        </div>
    </main>

</body>
</html>