<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, viewport-fit=cover">
    <title>Apugo | Village Square</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">

    <style>
        body { 
            background: radial-gradient(circle at top right, #0f172a, #020617); 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            color: #e2e8f0; 
            -webkit-tap-highlight-color: transparent;
        }
        .glass-nav {
            background: rgba(2, 6, 23, 0.8);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border-bottom: 1px solid rgba(255,255,255,0.03);
        }
        .post-card {
            background: rgba(15, 23, 42, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .post-card:hover {
            border-color: rgba(59, 130, 246, 0.4);
            background: rgba(15, 23, 42, 0.6);
        }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        
        .comment-section { display: none; }
        .comment-section.open { display: block; }

        .vibe-pop { animation: heartPop 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
        @keyframes heartPop {
            0% { transform: scale(1); }
            50% { transform: scale(1.4); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body class="min-h-screen pb-32">

    <nav class="glass-nav sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between gap-6">
            <h1 class="text-2xl font-black text-blue-500 italic tracking-tighter cursor-pointer" onclick="window.location.href='/feed'">APUGO</h1>
            <div class="flex items-center gap-6">
                <a href="/forum" class="hidden md:block text-[10px] font-black uppercase text-blue-400 tracking-widest">Town Hall</a>
                <a href="/profile" class="w-8 h-8 rounded-full overflow-hidden border border-white/10">
                    <img src="<%= user.profile_pic || '/default-avatar.png' %>" class="w-full h-full object-cover">
                </a>
            </div>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-4 mt-8 grid grid-cols-1 lg:grid-cols-12 gap-10">
        
        <div class="lg:col-span-8 space-y-8">
            
            <section class="bg-slate-900/50 border border-white/5 rounded-[2.5rem] p-6 lg:p-8">
                <form action="/event/create" method="POST" enctype="multipart/form-data" id="postForm">
                    <textarea name="description" id="postContent" placeholder="What's the word in the square?" required 
                        class="w-full bg-transparent outline-none h-20 resize-none text-[16px] text-slate-200 placeholder-slate-600 font-medium"></textarea>
                    
                    <div id="mediaPreviewContainer" class="hidden mt-4 relative w-32 h-32 rounded-2xl overflow-hidden border border-white/10">
                        <img id="imagePreview" class="w-full h-full object-cover">
                        <button type="button" onclick="clearMedia()" class="absolute top-2 right-2 bg-black/60 text-white w-5 h-5 rounded-full text-[10px]"><i class="fas fa-times"></i></button>
                    </div>

                    <div class="flex items-center justify-between border-t border-white/5 mt-6 pt-6">
                        <label for="fileInput" class="cursor-pointer bg-white/5 px-5 py-3 rounded-2xl text-[10px] font-black uppercase tracking-widest text-slate-300">
                            <i class="fas fa-camera text-blue-500 mr-2"></i> Media
                            <input type="file" name="localMedia" id="fileInput" class="hidden" accept="image/*,video/*">
                        </label>
                        <button type="submit" id="publishBtn" class="bg-blue-600 px-10 py-3.5 rounded-2xl font-black text-[10px] text-white uppercase tracking-widest">Publish</button>
                    </div>
                </form>
            </section>

            <div class="space-y-6" id="feedContainer">
                <% if (locals.posts && posts.length > 0) { %>
                    <% posts.forEach(p => { %>
                        <article class="post-card rounded-[2.5rem] overflow-hidden" id="post-<%= p.id %>">
                            <div class="p-6">
                                <p class="font-bold text-[14px] text-white">@<%= p.author ? p.author.split('@')[0] : 'villager' %></p>
                                <p class="text-[8px] text-slate-600 uppercase font-black tracking-widest"><%= new Date(p.created_at).toLocaleDateString() %></p>
                            </div>

                            <div class="px-8 pb-5 text-slate-300 text-[15px] leading-relaxed"><%= p.description %></div>

                            <% if (p.image_url) { %>
                                <div class="px-4 pb-4">
                                    <div class="rounded-[2rem] overflow-hidden bg-black/20 border border-white/5">
                                        <img src="<%= p.image_url %>" class="w-full h-auto max-h-[600px] object-cover">
                                    </div>
                                </div>
                            <% } %>

                            <div class="px-8 py-5 border-t border-white/5 flex gap-8">
                                <button onclick="sendVibe(this, '<%= p.id %>')" class="flex items-center gap-2 text-[10px] font-black uppercase text-slate-500 <%= p.liked_by_me ? 'text-red-500' : '' %>">
                                    <i class="<%= p.liked_by_me ? 'fas' : 'far' %> fa-heart text-lg"></i> 
                                    <span class="vibe-count"><%= p.likes_count || 0 %></span>
                                </button>
                                <button onclick="toggleEchoes('<%= p.id %>')" class="flex items-center gap-2 text-[10px] font-black uppercase text-slate-500">
                                    <i class="far fa-comment-dots text-lg"></i> 
                                    <span><%= (p.comments && p.comments.length) || 0 %> Echoes</span>
                                </button>
                            </div>

                            <div id="echoes-<%= p.id %>" class="comment-section bg-black/20 border-t border-white/5">
                                <div class="p-6 space-y-4 max-h-60 overflow-y-auto no-scrollbar">
                                    <% if (p.comments && p.comments.length > 0) { %>
                                        <% p.comments.forEach(c => { %>
                                            <div class="bg-white/5 rounded-2xl p-3 border border-white/5">
                                                <p class="text-[9px] font-black text-blue-500">@<%= c.username || 'villager' %></p>
                                                <p class="text-xs text-slate-300"><%= c.comment_text || c.content %></p>
                                            </div>
                                        <% }) %>
                                    <% } %>
                                </div>
                                <form action="/event/<%= p.id %>/comment" method="POST" class="p-4 flex gap-3">
                                    <input type="text" name="comment" placeholder="Add an echo..." class="flex-1 bg-slate-900 border border-white/10 rounded-xl px-4 py-2 text-xs text-white outline-none">
                                    <button class="bg-blue-600 w-10 h-10 rounded-xl text-white"><i class="fas fa-paper-plane"></i></button>
                                </form>
                            </div>
                        </article>
                    <% }) %>
                <% } %>
            </div>

            <div id="scrollLoader" class="py-10 text-center hidden">
                <i class="fas fa-circle-notch animate-spin text-blue-500 text-2xl"></i>
            </div>
        </div>

        <aside class="hidden lg:block lg:col-span-4">
            <div class="bg-slate-900/40 border border-white/5 rounded-[2.5rem] p-8 sticky top-24">
                <h3 class="text-[10px] font-black text-blue-500 uppercase tracking-[0.4em] mb-6">Hot Whispers</h3>
                <div class="space-y-4">
                    <p class="text-xs text-slate-500 italic">No trending whispers yet...</p>
                </div>
            </div>
        </aside>
    </main>

    <script>
      // --- EXISTING LOGIC ---
let page = 0;
let loading = false;
let hasMore = true;

function toggleEchoes(postId) {
    document.getElementById(`echoes-${postId}`).classList.toggle('open');
}

async function sendVibe(btn, postId) {
    const icon = btn.querySelector('i');
    const countSpan = btn.querySelector('.vibe-count');
    const isLiked = icon.classList.contains('fas');
    let count = parseInt(countSpan.innerText);

    if (isLiked) {
        icon.classList.replace('fas', 'far');
        btn.classList.replace('text-red-500', 'text-slate-500');
        countSpan.innerText = Math.max(0, count - 1);
    } else {
        icon.classList.replace('far', 'fas');
        btn.classList.replace('text-slate-500', 'text-red-500');
        countSpan.innerText = count + 1;
        btn.classList.add('vibe-pop');
        setTimeout(() => btn.classList.remove('vibe-pop'), 400);
    }

    try {
        const response = await fetch(`/event/${postId}/like`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' }
        });
        const data = await response.json();
        if (data.success) countSpan.innerText = data.newCount;
    } catch (err) { console.error("Vibe sync failed", err); }
}

// --- NEW INFINITE SCROLL LOGIC ---

window.onscroll = async function() {
    // If user is 800px from the bottom, load more
    if ((window.innerHeight + window.scrollY) >= document.body.offsetHeight - 800 && !loading && hasMore) {
        loading = true;
        page++;
        
        // Optional: show a small loader at bottom
        console.log(`Fetching page ${page}...`);

        try {
            const response = await fetch(`/feed?page=${page}`, {
                headers: { 'X-Requested-With': 'XMLHttpRequest' }
            });
            const data = await response.json();

            if (!data.posts || data.posts.length === 0) {
                hasMore = false;
                return;
            }

            data.posts.forEach(post => {
                appendPostToFeed(post);
            });
            
            loading = false;
        } catch (err) {
            console.error("Scroll fetch failed", err);
            loading = false;
        }
    }
};

function appendPostToFeed(p) {
    const container = document.getElementById('feedContainer');
    const date = new Date(p.created_at).toLocaleDateString();
    const authorHandle = p.author ? p.author.split('@')[0] : 'villager';
    
    // This matches your refactored "post-card" structure exactly
    const postHTML = `
        <article class="post-card rounded-[2.5rem] overflow-hidden" id="post-${p.id}">
            <div class="p-6 flex justify-between items-center">
                <div class="flex items-center gap-4">
                    <div class="avatar-container bg-slate-800 rounded-2xl overflow-hidden ring-1 ring-white/10">
                        <img src="${p.profile_pic || '/default-avatar.png'}" class="w-full h-full object-cover">
                    </div>
                    <div>
                        <div class="flex items-center gap-2">
                            <p class="font-bold text-[14px] text-white">@${authorHandle}</p>
                            ${p.is_verified ? '<i class="fas fa-certificate text-blue-500 text-[9px]"></i>' : ''}
                        </div>
                        <p class="text-[8px] text-slate-600 uppercase font-black tracking-widest">${date}</p>
                    </div>
                </div>
            </div>

            <div class="px-8 pb-5 text-slate-300 text-[15px] leading-relaxed">
                ${p.description}
            </div>

            ${p.image_url ? `
                <div class="px-4 pb-4">
                    <div class="rounded-[2rem] overflow-hidden bg-black/20 border border-white/5">
                        ${(p.media_type === 'video' || p.image_url.includes('.mp4')) 
                            ? `<video controls class="w-full max-h-[500px] object-cover"><source src="${p.image_url}" type="video/mp4"></video>`
                            : `<img src="${p.image_url}" class="w-full h-auto max-h-[600px] object-cover" loading="lazy">`
                        }
                    </div>
                </div>
            ` : ''}

            <div class="px-8 py-5 border-t border-white/5 bg-white/[0.01] flex gap-8">
                <button type="button" onclick="sendVibe(this, '${p.id}')" 
                    class="vibe-btn flex items-center gap-2 text-[10px] font-black uppercase tracking-widest transition-all ${p.liked_by_me ? 'text-red-500' : 'text-slate-500 hover:text-red-400'}">
                    <i class="${p.liked_by_me ? 'fas' : 'far'} fa-heart text-lg"></i> 
                    <span class="vibe-count">${p.likes_count || 0}</span>
                </button>
                
                <button onclick="toggleEchoes('${p.id}')" class="flex items-center gap-2 text-[10px] font-black uppercase tracking-widest text-slate-500 hover:text-blue-400">
                    <i class="far fa-comment-dots text-lg"></i> 
                    <span>${p.comments_list ? p.comments_list.length : 0} Echoes</span>
                </button>
            </div>

            <div id="echoes-${p.id}" class="comment-section bg-black/20 border-t border-white/5">
                <form action="/event/${p.id}/comment" method="POST" class="p-4 flex gap-3 border-t border-white/5">
                    <input type="text" name="comment" placeholder="Add an echo..." class="flex-1 bg-slate-900 border border-white/10 rounded-xl px-4 py-2 text-xs text-white outline-none">
                    <button class="bg-blue-600 w-10 h-10 rounded-xl text-[10px]"><i class="fas fa-paper-plane"></i></button>
                </form>
            </div>
        </article>
    `;
    
    container.insertAdjacentHTML('beforeend', postHTML);
    }
    </script>
</body>
</html>