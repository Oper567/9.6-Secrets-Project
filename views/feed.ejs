<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, viewport-fit=cover">
    <title>Apugo | Village Square</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">

    <style>
        body { 
            background: radial-gradient(circle at top right, #0f172a, #020617); 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            color: #e2e8f0; 
            -webkit-tap-highlight-color: transparent;
        }
        .glass-nav {
            background: rgba(2, 6, 23, 0.8);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border-bottom: 1px solid rgba(255,255,255,0.03);
        }
        .post-card {
            background: rgba(15, 23, 42, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .post-card:hover {
            border-color: rgba(59, 130, 246, 0.4);
            background: rgba(15, 23, 42, 0.6);
        }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        
        .comment-section {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.4s ease-out;
        }
        .comment-section.open {
            max-height: 1000px;
        }
        .user-initial {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 800;
            color: white;
            text-transform: uppercase;
        }
    </style>
</head>
<body class="min-h-screen pb-32">

    <nav class="glass-nav sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between gap-6">
            <div class="flex items-center gap-6">
                <h1 class="text-2xl font-black text-blue-500 italic tracking-tighter cursor-pointer hover:scale-105 transition-transform" onclick="window.location.href='/feed'">
                    APUGO
                </h1>
                <div class="hidden lg:block w-80 relative group">
                    <form action="/feed" method="GET">
                        <input type="text" name="search" placeholder="Search whispers..." value="<%= locals.search || '' %>" 
                            class="w-full bg-white/5 border border-white/5 rounded-2xl py-2 px-10 text-[11px] font-bold uppercase tracking-widest outline-none focus:border-blue-500/50 transition-all text-white">
                        <i class="fas fa-search absolute left-4 top-2.5 text-slate-600 text-[10px]"></i>
                    </form>
                </div>
            </div>

            <div class="flex items-center gap-6">
                <a href="/forum" class="hidden md:flex items-center gap-2 text-[10px] font-black uppercase text-blue-400 hover:text-white transition-all tracking-widest">
                    <i class="fas fa-landmark"></i> Town Hall
                </a>
                <a href="/notifications" class="relative group">
                    <i class="far fa-bell text-xl text-slate-400 group-hover:text-blue-500 transition-colors"></i>
                    <% if (locals.unreadCount && unreadCount > 0) { %>
                        <span class="absolute -top-1 -right-1 bg-blue-600 text-[8px] w-4 h-4 flex items-center justify-center rounded-full font-black border-2 border-[#020617]"><%= unreadCount %></span>
                    <% } %>
                </a>
                <a href="/profile" class="flex items-center gap-2 bg-white/5 px-4 py-2 rounded-full border border-white/5 text-slate-300">
                    <i class="fas fa-user-circle"></i>
                    <span class="text-[9px] font-black uppercase tracking-widest">Profile</span>
                </a>
            </div>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-4 mt-6 lg:mt-8 grid grid-cols-1 lg:grid-cols-12 gap-6 lg:gap-10">
        
        <div class="lg:col-span-8 space-y-8">
            
            <section class="bg-slate-900/50 border border-white/5 rounded-[2.5rem] p-6 lg:p-8 shadow-2xl">
                <form action="/event/create" method="POST" enctype="multipart/form-data" id="postForm">
                    <div class="flex gap-4">
                        <div class="w-full">
                            <textarea name="description" id="postContent" placeholder="What's the word in the square?" required 
                                class="w-full bg-transparent outline-none h-20 resize-none text-[16px] text-slate-200 placeholder-slate-600 mt-1 leading-relaxed font-medium"></textarea>
                            
                            <div id="mediaPreviewContainer" class="hidden mt-4 relative w-32 h-32 rounded-2xl overflow-hidden border border-white/10 group">
                                <img id="imagePreview" class="w-full h-full object-cover">
                                <button type="button" onclick="clearMedia()" class="absolute top-2 right-2 bg-black/60 text-white w-6 h-6 rounded-full text-[10px] hover:bg-red-500 transition-colors">
                                    <i class="fas fa-times"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                    
                    <div class="flex items-center justify-between border-t border-white/5 mt-6 pt-6">
                        <div class="flex gap-2">
                            <input type="file" name="localMedia" id="fileInput" class="hidden" accept="image/*,video/*">
                            <label for="fileInput" class="cursor-pointer bg-white/5 hover:bg-blue-600/10 text-slate-300 px-5 py-3 rounded-2xl text-[10px] font-black uppercase flex items-center gap-3 tracking-widest transition-all">
                                <i class="fas fa-camera text-blue-500"></i> Media
                            </label>
                        </div>
                        <button type="submit" class="bg-blue-600 hover:bg-blue-500 px-10 py-3.5 rounded-2xl font-black text-[10px] text-white uppercase tracking-[0.2em] transition-all">
                            Publish
                        </button>
                    </div>
                </form>
            </section>

            <div class="space-y-6" id="feedContainer">
                <% if (locals.posts && posts.length > 0) { %>
                    <% posts.forEach(p => { %>
                        <article class="post-card rounded-[2.5rem] overflow-hidden" id="post-<%= p.id %>">
                            <div class="p-6 flex justify-between items-center">
                                <div class="flex items-center gap-3">
                                    <div class="w-10 h-10 rounded-2xl user-initial text-sm">
                                        <%= p.author ? p.author.charAt(0) : 'V' %>
                                    </div>
                                    <div>
                                        <div class="flex items-center gap-2">
                                            <p class="font-bold text-[14px] text-white">@<%= p.author ? p.author.split('@')[0] : 'villager' %></p>
                                            <% if (p.is_verified) { %> <i class="fas fa-certificate text-blue-500 text-[9px]"></i> <% } %>
                                        </div>
                                        <p class="text-[8px] text-slate-600 uppercase font-black tracking-widest"><%= new Date(p.created_at).toLocaleDateString() %></p>
                                    </div>
                                </div>
                            </div>

                            <div class="px-8 pb-5 text-slate-300 text-[15px] leading-relaxed">
                                <%= p.description %>
                            </div>

                            <% if (p.image_url) { %>
                                <div class="px-4 pb-4">
                                    <div class="rounded-[2rem] overflow-hidden bg-black/20 border border-white/5">
                                        <% if (p.media_type === 'video' || p.image_url.includes('.mp4')) { %>
                                            <video controls class="w-full max-h-[500px] object-cover"><source src="<%= p.image_url %>" type="video/mp4"></video>
                                        <% } else { %>
                                            <img src="<%= p.image_url %>" class="w-full h-auto max-h-[600px] object-cover" loading="lazy">
                                        <% } %>
                                    </div>
                                </div>
                            <% } %>

                            <div class="px-8 py-5 border-t border-white/5 bg-white/[0.01] flex gap-8">
                                <button type="button" onclick="sendVibe(this, '<%= p.id %>')" 
                                    class="flex items-center gap-2 text-[10px] font-black uppercase tracking-widest transition-all <%= p.liked_by_me ? 'text-red-500' : 'text-slate-500' %>">
                                    <i class="<%= p.liked_by_me ? 'fas' : 'far' %> fa-heart text-lg"></i> 
                                    <span class="vibe-count"><%= p.likes_count || 0 %></span>
                                </button>
                                
                                <button onclick="toggleEchoes('<%= p.id %>')" class="flex items-center gap-2 text-[10px] font-black uppercase tracking-widest text-slate-500 hover:text-blue-400">
                                    <i class="far fa-comment-dots text-lg"></i> 
                                    <span><%= p.comments_list ? p.comments_list.length : 0 %> Echoes</span>
                                </button>
                            </div>

                            <div id="echoes-<%= p.id %>" class="comment-section bg-black/20 border-t border-white/5">
                                <div class="p-6 space-y-4 max-h-64 overflow-y-auto no-scrollbar">
                                    <% if (p.comments_list && p.comments_list.length > 0) { %>
                                        <% p.comments_list.forEach(c => { %>
                                            <div class="flex items-start gap-3">
                                                <div class="w-7 h-7 rounded-lg user-initial text-[10px] flex-shrink-0">
                                                    <%= c.username ? c.username.charAt(0) : '?' %>
                                                </div>
                                                <div class="bg-white/5 rounded-2xl p-3 flex-1">
                                                    <p class="text-[9px] font-black text-blue-500 uppercase">@<%= c.username %></p>
                                                    <p class="text-xs text-slate-300 mt-1"><%= c.comment_text %></p>
                                                </div>
                                            </div>
                                        <% }) %>
                                    <% } else { %>
                                        <p class="text-center text-[10px] text-slate-600 font-bold italic py-4">No echoes yet...</p>
                                    <% } %>
                                </div>
                                <form action="/event/<%= p.id %>/comment" method="POST" class="p-4 flex gap-3 border-t border-white/5">
                                    <input type="text" name="comment" placeholder="Add an echo..." required 
                                        class="flex-1 bg-slate-900 border border-white/10 rounded-xl px-4 py-2 text-xs text-white outline-none focus:border-blue-500/40">
                                    <button class="bg-blue-600 w-10 h-10 rounded-xl flex items-center justify-center"><i class="fas fa-paper-plane text-xs"></i></button>
                                </form>
                            </div>
                        </article>
                    <% }) %>
                <% } %>
            </div>
        </div>

        <aside class="hidden lg:block lg:col-span-4 space-y-8">
            <div class="bg-slate-900/40 border border-white/5 rounded-[2.5rem] p-8">
                <h3 class="text-[10px] font-black text-blue-500 uppercase tracking-[0.4em] mb-6">Your Kin</h3>
                <div class="space-y-4">
                    <% if (locals.friends && friends.length > 0) { %>
                        <% friends.slice(0, 5).forEach(f => { %>
                            <div class="flex items-center gap-3">
                                <div class="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center text-[10px] font-bold text-slate-500">
                                    <%= (f.username || f.email).charAt(0).toUpperCase() %>
                                </div>
                                <p class="text-[11px] font-bold text-slate-300">@<%= (f.username || f.email).split('@')[0] %></p>
                            </div>
                        <% }) %>
                    <% } %>
                </div>
            </div>
        </aside>
    </main>

    <div class="lg:hidden fixed bottom-6 left-1/2 -translate-x-1/2 bg-slate-900/90 backdrop-blur-2xl border border-white/10 px-8 py-4 rounded-full flex gap-10 shadow-2xl z-50 text-slate-500">
        <a href="/feed" class="text-blue-500"><i class="fas fa-home"></i></a>
        <a href="/forum"><i class="fas fa-landmark"></i></a>
        <a href="/profile"><i class="fas fa-user"></i></a>
    </div>

    <script>
        let page = 0;
        let loading = false;
        let hasMore = true;

        function toggleEchoes(postId) {
            document.getElementById(`echoes-${postId}`).classList.toggle('open');
        }

        async function sendVibe(btn, postId) {
            const icon = btn.querySelector('i');
            const countSpan = btn.querySelector('.vibe-count');
            const isLiked = icon.classList.contains('fas');
            
            icon.classList.toggle('fas');
            icon.classList.toggle('far');
            btn.classList.toggle('text-red-500');
            btn.classList.toggle('text-slate-500');

            try {
                const response = await fetch(`/event/${postId}/like`, { method: 'POST' });
                const data = await response.json();
                if (data.success) countSpan.innerText = data.newCount;
            } catch (err) { console.error(err); }
        }

        window.onscroll = async function() {
            if ((window.innerHeight + window.scrollY) >= document.body.offsetHeight - 800 && !loading && hasMore) {
                loading = true;
                page++;
                try {
                    const res = await fetch(`/feed?page=${page}`, { headers: { 'X-Requested-With': 'XMLHttpRequest' } });
                    const data = await res.json();
                    if (!data.posts || data.posts.length === 0) hasMore = false;
                    else data.posts.forEach(p => appendPostToFeed(p));
                    loading = false;
                } catch (e) { loading = false; }
            }
        };

        function appendPostToFeed(p) {
            const container = document.getElementById('feedContainer');
            const authorInitial = p.author ? p.author.charAt(0).toUpperCase() : 'V';
            
            let commentsHTML = '';
            if (p.comments_list && p.comments_list.length > 0) {
                p.comments_list.forEach(c => {
                    commentsHTML += `
                        <div class="flex items-start gap-3 mb-3">
                            <div class="w-7 h-7 rounded-lg user-initial text-[10px] flex-shrink-0">${c.username ? c.username.charAt(0).toUpperCase() : '?'}</div>
                            <div class="bg-white/5 rounded-2xl p-3 flex-1">
                                <p class="text-[9px] font-black text-blue-500 uppercase">@${c.username}</p>
                                <p class="text-xs text-slate-300 mt-1">${c.comment_text}</p>
                            </div>
                        </div>`;
                });
            } else {
                commentsHTML = '<p class="text-center text-[10px] text-slate-600 font-bold italic py-4">No echoes yet...</p>';
            }

            const html = `
                <article class="post-card rounded-[2.5rem] overflow-hidden mb-6" id="post-${p.id}">
                    <div class="p-6 flex items-center gap-3">
                        <div class="w-10 h-10 rounded-2xl user-initial text-sm">${authorInitial}</div>
                        <div>
                            <p class="font-bold text-[14px] text-white">@${p.author ? p.author.split('@')[0] : 'villager'}</p>
                            <p class="text-[8px] text-slate-600 uppercase font-black">${new Date(p.created_at).toLocaleDateString()}</p>
                        </div>
                    </div>
                    <div class="px-8 pb-5 text-slate-300 text-[15px]">${p.description}</div>
                    <div class="px-8 py-5 border-t border-white/5 flex gap-8">
                        <button onclick="sendVibe(this, '${p.id}')" class="flex items-center gap-2 text-[10px] font-black text-slate-500">
                            <i class="far fa-heart text-lg"></i> <span class="vibe-count">${p.likes_count || 0}</span>
                        </button>
                        <button onclick="toggleEchoes('${p.id}')" class="text-[10px] font-black text-slate-500">
                            <i class="far fa-comment-dots text-lg"></i> Echoes
                        </button>
                    </div>
                    <div id="echoes-${p.id}" class="comment-section bg-black/20 border-t border-white/5">
                        <div class="p-6">${commentsHTML}</div>
                        <form action="/event/${p.id}/comment" method="POST" class="p-4 flex gap-3">
                            <input type="text" name="comment" placeholder="Add an echo..." required class="flex-1 bg-slate-900 border border-white/10 rounded-xl px-4 py-2 text-xs text-white outline-none">
                            <button class="bg-blue-600 w-10 h-10 rounded-xl flex items-center justify-center"><i class="fas fa-paper-plane text-xs"></i></button>
                        </form>
                    </div>
                </article>`;
            container.insertAdjacentHTML('beforeend', html);
        }
    </script>
</body>
</html>