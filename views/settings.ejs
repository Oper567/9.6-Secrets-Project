<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apugo | Settings</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #020617; }
        .glass-nav { background: rgba(15, 23, 42, 0.8); backdrop-filter: blur(12px); }
    </style>
</head>
<body class="text-slate-200 min-h-screen">

    <nav class="glass-nav border-b border-slate-800 p-4 sticky top-0 z-50">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-black text-blue-500 italic tracking-tighter cursor-pointer" onclick="window.location.href='/feed'">APUGO</h1>
            <div class="flex items-center gap-6">
                <div class="flex gap-6 text-xs font-bold uppercase tracking-widest">
                    <a href="/feed" class="hover:text-blue-400 transition">Feed</a>
                    <a href="/profile" class="hover:text-blue-400 transition">Profile</a>
                    <a href="/settings" class="text-blue-500">Settings</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-xl mx-auto px-4 pt-16">
        <div class="bg-slate-900 border border-slate-800 rounded-3xl p-8 shadow-2xl">
            <h2 class="text-2xl font-black text-white mb-2">Account Settings</h2>
            <p class="text-slate-500 text-sm mb-8">Update your credentials below.</p>

            <form action="/settings/update" method="POST" class="space-y-6">
                <div>
                    <label class="block text-xs font-black uppercase tracking-widest text-slate-400 mb-2">Email Address</label>
                    <input type="email" name="email" value="<%= user.email %>" required 
                        class="w-full bg-slate-950 border border-slate-800 rounded-xl px-4 py-3 text-white outline-none focus:border-blue-600 transition">
                </div>

                <div>
                    <label class="block text-xs font-black uppercase tracking-widest text-slate-400 mb-2">New Password (Leave blank to keep current)</label>
                    <input type="password" name="newPassword" placeholder="••••••••" 
                        class="w-full bg-slate-950 border border-slate-800 rounded-xl px-4 py-3 text-white outline-none focus:border-blue-600 transition">
                </div>

                <div class="pt-4">
                    <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white font-black py-4 rounded-xl transition shadow-lg shadow-blue-900/20">
                        SAVE CHANGES
                    </button>
                </div>
            </form>

            <div class="mt-8 pt-8 border-t border-slate-800">
                <a href="/logout" class="text-red-500 font-bold text-sm hover:underline">Sign out of all sessions</a>
            </div>
        </div>
    </main>

</body>
</html>