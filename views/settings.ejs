<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <title>Apugo | Soul Settings</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;800&display=swap');
        body { 
            background-color: #020617; 
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: #e2e8f0;
        }
        .glass-nav { background: rgba(15, 23, 42, 0.9); backdrop-filter: blur(20px); }
        .input-field {
            background: #0f172a;
            border: 1px solid #1e293b;
            transition: all 0.3s ease;
        }
        .input-field:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 15px rgba(59, 130, 246, 0.1);
        }
    </style>
</head>
<body class="pb-32">

    <nav class="glass-nav border-b border-slate-800 p-4 sticky top-0 z-50">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
            <h1 class="text-xl font-black text-blue-500 italic tracking-tighter cursor-pointer" onclick="window.location.href='/feed'">APUGO</h1>
            <a href="/logout" class="text-[9px] font-black text-slate-500 uppercase border border-slate-800 px-3 py-1.5 rounded-full hover:bg-red-900/20 hover:text-red-400 transition-all">Exit</a>
        </div>
    </nav>

    <main class="max-w-xl mx-auto px-6 pt-12">
        <header class="mb-10">
            <h2 class="text-4xl font-black italic tracking-tighter text-white uppercase">Settings</h2>
            <div class="flex items-center gap-2 mt-2">
                <div class="h-0.5 w-8 bg-blue-600"></div>
                <p class="text-slate-500 text-[10px] font-black uppercase tracking-widest">Calibrate your presence</p>
            </div>
        </header>

        <form action="/settings/update" method="POST" class="space-y-10">
            
            <section class="space-y-6">
                <h3 class="text-[10px] font-black text-blue-500 uppercase tracking-[0.3em]">Soul Identity</h3>
                
                <div class="flex flex-col items-center mb-8">
                    <div class="relative group">
                        <div class="w-24 h-24 rounded-full overflow-hidden border-2 border-slate-800 bg-slate-900 shadow-2xl">
                            <img id="previewImg" src="<%= user.profile_pic || 'https://ui-avatars.com/api/?name=' + user.email %>" 
                                 class="w-full h-full object-cover">
                        </div>
                        <div class="absolute inset-0 flex items-center justify-center bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity rounded-full">
                            <i class="fas fa-camera text-white text-sm"></i>
                        </div>
                    </div>
                    <p class="text-[8px] text-slate-600 mt-2 font-black uppercase">Live Identity Preview</p>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="block text-[9px] font-black uppercase tracking-widest text-slate-500 mb-2 ml-1">Avatar Image URL</label>
                        <input type="text" name="profile_pic" value="<%= user.profile_pic %>" 
                            oninput="document.getElementById('previewImg').src = this.value"
                            placeholder="https://images.com/your-soul.jpg" 
                            class="input-field w-full rounded-2xl px-5 py-3.5 text-sm text-white outline-none">
                    </div>

                    <div>
                        <label class="block text-[9px] font-black uppercase tracking-widest text-slate-500 mb-2 ml-1">Soul Description (Bio)</label>
                        <textarea name="bio" rows="3" placeholder="Tell the village your story..."
                            class="input-field w-full rounded-2xl px-5 py-3.5 text-sm text-white outline-none resize-none"><%= user.bio %></textarea>
                    </div>
                </div>
            </section>

            <section class="space-y-6 pt-6 border-t border-slate-800/50">
                <h3 class="text-[10px] font-black text-blue-500 uppercase tracking-[0.3em]">Credentials</h3>
                
                <div class="space-y-4">
                    <div>
                        <label class="block text-[9px] font-black uppercase tracking-widest text-slate-500 mb-2 ml-1">Email Address</label>
                        <input type="email" name="email" value="<%= user.email %>" required 
                            class="input-field w-full rounded-2xl px-5 py-3.5 text-sm text-white outline-none">
                    </div>

                    <div>
                        <label class="block text-[9px] font-black uppercase tracking-widest text-slate-500 mb-2 ml-1">Update Password</label>
                        <input type="password" name="newPassword" placeholder="Keep current or enter new" 
                            class="input-field w-full rounded-2xl px-5 py-3.5 text-sm text-white outline-none">
                    </div>
                </div>
            </section>

            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white text-[11px] font-black uppercase tracking-[0.2em] py-5 rounded-2xl transition-all shadow-xl shadow-blue-600/10 active:scale-95">
                Commit Changes
            </button>
        </form>

        <div class="mt-12 text-center pb-12">
            <a href="/logout" class="text-red-500/50 hover:text-red-500 text-[9px] font-black uppercase tracking-widest transition-colors">
                Permanently Clear All Sessions
            </a>
        </div>
    </main>

    <nav class="fixed bottom-6 left-1/2 -translate-x-1/2 bg-slate-900/80 backdrop-blur-xl border border-white/10 px-8 py-4 rounded-full flex gap-10 shadow-2xl z-50">
        <a href="/feed" class="text-slate-400 hover:text-white transition-all"><i class="fas fa-home"></i></a>
        <a href="/discover" class="text-slate-400 hover:text-white transition-all"><i class="fas fa-compass"></i></a>
        <a href="/messages" class="text-slate-400 hover:text-white transition-all"><i class="fas fa-comment-dots"></i></a>
        <a href="/profile" class="text-blue-500 transition-all"><i class="fas fa-user"></i></a>
    </nav>

</body>
</html>