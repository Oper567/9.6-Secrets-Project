<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, viewport-fit=cover">
    <title>Apugo | Town Hall</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">

    <style>
        body { 
            background: radial-gradient(circle at top right, #020617, #0f172a); 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            color: #e2e8f0; 
        }
        .glass-nav {
            background: rgba(2, 6, 23, 0.8);
            backdrop-filter: blur(24px);
            border-bottom: 1px solid rgba(255,255,255,0.03);
        }
        .forum-card {
            background: rgba(30, 41, 59, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.2s ease;
        }
        .forum-card:hover {
            border-color: rgba(59, 130, 246, 0.3);
            background: rgba(30, 41, 59, 0.5);
        }
        .category-pill {
            background: rgba(59, 130, 246, 0.1);
            color: #60a5fa;
            border: 1px solid rgba(59, 130, 246, 0.2);
        }
        .category-pill.active {
            background: #2563eb;
            color: white;
        }
        .no-scrollbar::-webkit-scrollbar { display: none; }
    </style>
</head>
<body class="min-h-screen pb-20">

    <nav class="glass-nav sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
            <div class="flex items-center gap-6">
                <h1 class="text-2xl font-black text-blue-500 italic tracking-tighter cursor-pointer" onclick="window.location.href='/feed'">APUGO</h1>
                <div class="hidden md:flex items-center gap-2 text-[10px] font-black uppercase text-slate-500 tracking-widest">
                    <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
                    Town Hall Active
                </div>
            </div>
            <div class="flex items-center gap-6">
                <a href="/feed" class="text-slate-400 hover:text-white text-sm font-bold transition-colors">Back to Square</a>
                <a href="/profile" class="w-8 h-8 rounded-full overflow-hidden border border-white/10">
                    <img src="<%= user.profile_pic || '/default-avatar.png' %>" class="w-full h-full object-cover">
                </a>
            </div>
        </div>
    </nav>

    <main class="max-w-5xl mx-auto px-4 mt-8">
        <div class="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-10">
            <div>
                <p class="text-[10px] font-black text-blue-500 uppercase tracking-[0.4em] mb-2">The Great Hall</p>
                <h2 class="text-4xl font-extrabold text-white tracking-tight">Village Forum</h2>
                <p class="text-slate-400 mt-2 text-sm">Organized discussions, trades, and village legislation.</p>
            </div>
            <button onclick="toggleModal()" class="bg-blue-600 hover:bg-blue-500 text-white px-8 py-3.5 rounded-2xl font-black text-[10px] uppercase tracking-widest shadow-lg shadow-blue-600/20 transition-all active:scale-95">
                Start Discussion
            </button>
        </div>

        <div class="flex gap-3 overflow-x-auto pb-6 no-scrollbar">
            <a href="/forum" class="category-pill <%= !activeCat || activeCat === 'all' ? 'active' : '' %> shrink-0 px-6 py-2.5 rounded-xl text-[10px] font-black uppercase tracking-widest transition-all">All Topics</a>
            <% ['marketplace', 'security', 'festivals', 'disputes'].forEach(cat => { %>
                <a href="/forum?cat=<%= cat %>" class="category-pill <%= activeCat === cat ? 'active' : '' %> shrink-0 px-6 py-2.5 rounded-xl text-[10px] font-black uppercase tracking-widest transition-all hover:bg-blue-600/20">
                    <%= cat.charAt(0).toUpperCase() + cat.slice(1) %>
                </a>
            <% }) %>
        </div>

        <div class="space-y-4">
            <% if (threads && threads.length > 0) { %>
                <% threads.forEach(topic => { %>
                    <div class="forum-card rounded-[2rem] p-6 cursor-pointer group" onclick="window.location.href='/forum/thread/<%= topic.id %>'">
                        <div class="flex items-start justify-between gap-4">
                            <div class="flex gap-5 flex-1">
                                <div class="w-12 h-12 rounded-2xl bg-slate-800 flex items-center justify-center shrink-0 border border-white/5 overflow-hidden">
                                    <img src="<%= topic.author_pic || '/default-avatar.png' %>" class="w-full h-full object-cover">
                                </div>
                                <div class="flex-1">
                                    <div class="flex items-center gap-3 mb-1">
                                        <span class="text-[8px] font-black uppercase px-2 py-0.5 rounded bg-blue-500/20 text-blue-400 tracking-widest"><%= topic.category %></span>
                                        <span class="text-[8px] font-black uppercase text-slate-600 tracking-widest"><%= new Date(topic.created_at).toLocaleDateString() %></span>
                                        <% if (topic.image_url) { %>
                                            <i class="fas fa-paperclip text-slate-600 text-[10px]"></i>
                                        <% } %>
                                    </div>
                                    <h3 class="text-lg font-bold text-white mb-2 leading-tight group-hover:text-blue-400 transition-colors"><%= topic.title %></h3>
                                    <p class="text-slate-400 text-xs line-clamp-1"><%= topic.content %></p>
                                </div>
                            </div>
                            
                            <div class="hidden md:flex items-center gap-8 text-right shrink-0">
                                <div class="space-y-1">
                                    <p class="text-white font-bold text-sm"><%= topic.reply_count || 0 %></p>
                                    <p class="text-[8px] font-black text-slate-600 uppercase tracking-widest">Replies</p>
                                </div>
                                <div class="space-y-1">
                                    <p class="text-white font-bold text-sm"><%= topic.view_count || 0 %></p>
                                    <p class="text-[8px] font-black text-slate-600 uppercase tracking-widest">Views</p>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="text-center py-20 border border-dashed border-white/10 rounded-[3rem]">
                    <i class="fas fa-feather text-4xl text-slate-800 mb-4"></i>
                    <p class="text-slate-500 font-black uppercase tracking-widest text-[10px]">The hall is silent. Speak up!</p>
                </div>
            <% } %>
        </div>
    </main>

    <div id="topicModal" class="fixed inset-0 z-[100] hidden bg-black/90 backdrop-blur-md flex items-center justify-center p-4">
        <div class="bg-slate-900 border border-white/10 w-full max-w-xl rounded-[3rem] p-8 shadow-2xl">
            <div class="flex justify-between items-center mb-8">
                <div class="flex items-center gap-3">
                    <div class="w-2 h-8 bg-blue-600 rounded-full"></div>
                    <h3 class="text-xl font-black text-white uppercase italic tracking-tighter">New Discussion</h3>
                </div>
                <button onclick="toggleModal()" class="text-slate-500 hover:text-white transition-colors p-2"><i class="fas fa-times"></i></button>
            </div>

            <form action="/forum/create" method="POST" enctype="multipart/form-data" id="forumForm" class="space-y-5">
                <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Category</label>
                    <select name="category" class="w-full bg-white/5 border border-white/5 rounded-xl px-4 py-3 text-sm text-white outline-none focus:border-blue-500 transition-all appearance-none">
                        <option value="marketplace">Marketplace</option>
                        <option value="security">Village Security</option>
                        <option value="festivals">Festivals</option>
                        <option value="general">General Discussion</option>
                    </select>
                </div>

                <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Subject</label>
                    <input type="text" name="title" placeholder="Topic headline..." required class="w-full bg-white/5 border border-white/5 rounded-xl px-4 py-3 text-sm text-white outline-none focus:border-blue-500 transition-all">
                </div>

                <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Message Body</label>
                    <textarea name="content" placeholder="Details of the discussion..." required class="w-full bg-white/5 border border-white/5 rounded-xl px-4 py-3 text-sm text-white h-32 resize-none outline-none focus:border-blue-500 transition-all"></textarea>
                </div>

                <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Attachment (Optional)</label>
                    <div class="relative group">
                        <input type="file" name="media" id="forumFile" accept="image/*" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10">
                        <div class="bg-white/5 border border-dashed border-white/10 rounded-xl p-4 text-center group-hover:border-blue-500/50 transition-all">
                            <p id="fileNameDisplay" class="text-[10px] text-slate-500 font-bold uppercase tracking-widest">
                                <i class="fas fa-cloud-upload-alt mr-2"></i> Add Visual Proof
                            </p>
                        </div>
                    </div>
                </div>

                <button type="submit" id="submitBtn" class="w-full bg-blue-600 text-white py-4 rounded-2xl font-black text-[10px] uppercase tracking-[0.2em] hover:bg-blue-500 transition-all shadow-lg shadow-blue-600/10">
                    Open Thread
                </button>
            </form>
        </div>
    </div>

    <script>
        function toggleModal() {
            const modal = document.getElementById('topicModal');
            modal.classList.toggle('hidden');
            document.body.style.overflow = modal.classList.contains('hidden') ? 'auto' : 'hidden';
        }

        // File name display logic
        document.getElementById('forumFile').addEventListener('change', function(e) {
            const fileName = e.target.files[0]?.name || "Add Visual Proof";
            document.getElementById('fileNameDisplay').innerText = fileName;
            document.getElementById('fileNameDisplay').classList.add('text-blue-400');
        });

        // Loading state on form submission
        document.getElementById('forumForm').addEventListener('submit', function() {
            const btn = document.getElementById('submitBtn');
            btn.innerHTML = '<i class="fas fa-circle-notch animate-spin mr-2"></i> Uploading to Square...';
            btn.classList.add('opacity-70', 'pointer-events-none');
        });
    </script>
</body>
</html>