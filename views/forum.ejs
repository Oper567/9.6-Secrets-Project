<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, viewport-fit=cover">
    <title>Apugo | Town Hall</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">

    <style>
        body { 
            background: radial-gradient(circle at top right, #020617, #0f172a); 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            color: #e2e8f0; 
            -webkit-tap-highlight-color: transparent;
        }
        .glass-nav {
            background: rgba(2, 6, 23, 0.8);
            backdrop-filter: blur(24px);
            border-bottom: 1px solid rgba(255,255,255,0.03);
        }
        .forum-card {
            background: rgba(30, 41, 59, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .forum-card:hover {
            border-color: rgba(59, 130, 246, 0.4);
            background: rgba(30, 41, 59, 0.5);
            transform: translateY(-2px);
        }
        .category-pill {
            background: rgba(59, 130, 246, 0.05);
            color: #64748b;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
        .category-pill.active {
            background: #2563eb;
            color: white;
            border-color: #2563eb;
            box-shadow: 0 4px 15px rgba(37, 99, 235, 0.3);
        }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        
        .initial-avatar {
            background: linear-gradient(135deg, #1e293b, #0f172a);
            color: #3b82f6;
            font-weight: 800;
            display: flex;
            align-items: center;
            justify-content: center;
            text-transform: uppercase;
            border: 1px solid rgba(59, 130, 246, 0.2);
        }
    </style>
</head>
<body class="min-h-screen pb-20">

    <nav class="glass-nav sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
            <div class="flex items-center gap-6">
                <h1 class="text-2xl font-black text-blue-500 italic tracking-tighter cursor-pointer" onclick="window.location.href='/feed'">APUGO</h1>
                <div class="hidden md:flex items-center gap-2 text-[10px] font-black uppercase text-slate-500 tracking-widest">
                    <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
                    Town Hall Active
                </div>
            </div>
            <div class="flex items-center gap-6">
                <a href="/feed" class="text-slate-400 hover:text-white text-[10px] font-black uppercase tracking-widest transition-colors">Back to Square</a>
                <a href="/profile" class="w-8 h-8 rounded-xl overflow-hidden border border-white/10 aspect-square initial-avatar">
                    <% if (user.profile_pic) { %>
                        <img src="<%= user.profile_pic %>" class="w-full h-full object-cover">
                    <% } else { %>
                        <span class="text-[10px]"><%= (user.username || user.email)[0] %></span>
                    <% } %>
                </a>
            </div>
        </div>
    </nav>

    <main class="max-w-5xl mx-auto px-4 mt-8">
        <div class="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-10">
            <div>
                <p class="text-[10px] font-black text-blue-500 uppercase tracking-[0.4em] mb-2">The Great Hall</p>
                <h2 class="text-4xl font-extrabold text-white tracking-tight">Village Forum</h2>
                <p class="text-slate-400 mt-2 text-sm font-medium">Official discussions, trades, and village decrees.</p>
            </div>
            <div class="flex gap-3">
                <button onclick="toggleModal()" class="bg-blue-600 hover:bg-blue-500 text-white px-8 py-3.5 rounded-2xl font-black text-[10px] uppercase tracking-widest shadow-lg shadow-blue-600/20 transition-all active:scale-95">
                    <i class="fas fa-plus mr-2"></i> Start Discussion
                </button>
            </div>
        </div>

        <div class="flex items-center gap-4 mb-8">
            <div class="flex gap-3 overflow-x-auto pb-2 no-scrollbar flex-1">
                <a href="/forum" class="category-pill <%= (!locals.activeCat || locals.activeCat === 'all') ? 'active' : '' %> shrink-0 px-6 py-3 rounded-xl text-[10px] font-black uppercase tracking-widest transition-all">
                    All Topics
                </a>
                <% ['marketplace', 'security', 'festivals', 'disputes'].forEach(cat => { %>
                    <a href="/forum?cat=<%= cat %>" class="category-pill <%= (locals.activeCat === cat) ? 'active' : '' %> shrink-0 px-6 py-3 rounded-xl text-[10px] font-black uppercase tracking-widest transition-all">
                        <%= cat.charAt(0).toUpperCase() + cat.slice(1) %>
                    </a>
                <% }) %>
            </div>
        </div>

        <div class="space-y-4">
            <% if (locals.threads && threads.length > 0) { %>
                <% threads.forEach(topic => { %>
                    <div class="forum-card rounded-[2.5rem] p-6 cursor-pointer group" onclick="window.location.href='/forum/thread/<%= topic.id %>'">
                        <div class="flex items-start justify-between gap-4">
                            <div class="flex gap-5 flex-1">
                                <div class="w-14 h-14 rounded-2xl flex items-center justify-center shrink-0 border border-white/5 overflow-hidden aspect-square initial-avatar">
                                    <% if (topic.author_pic && topic.author_pic !== 'null') { %>
                                        <img src="<%= topic.author_pic %>" class="w-full h-full object-cover">
                                    <% } else { %>
                                        <span class="text-lg"><%= (topic.author_username || "V")[0] %></span>
                                    <% } %>
                                </div>

                                <div class="flex-1">
                                    <div class="flex items-center gap-3 mb-2">
                                        <span class="text-[8px] font-black uppercase px-2.5 py-1 rounded-lg bg-blue-500/10 text-blue-400 border border-blue-500/10 tracking-widest">
                                            <%= topic.category %>
                                        </span>
                                        <span class="text-[8px] font-black uppercase text-slate-600 tracking-widest">
                                            <i class="far fa-clock mr-1"></i> <%= new Date(topic.created_at).toLocaleDateString() %>
                                        </span>
                                    </div>
                                    <h3 class="text-xl font-bold text-white mb-2 leading-tight group-hover:text-blue-400 transition-colors flex items-center gap-3">
                                        <%= topic.title %>
                                        <% if (topic.category === 'marketplace' && topic.price) { %>
                                            <span class="bg-green-500/10 text-green-400 text-[10px] px-3 py-1 rounded-full border border-green-500/20 font-black">
                                                ₦<%= Number(topic.price).toLocaleString() %>
                                            </span>
                                        <% } %>
                                    </h3>
                                    <p class="text-slate-400 text-sm line-clamp-1 font-medium"><%= topic.content %></p>
                                </div>
                            </div>
                            
                            <div class="hidden sm:flex items-center gap-8 text-right shrink-0 px-4">
                                <div class="space-y-1">
                                    <p class="text-white font-bold text-sm"><%= topic.reply_count || 0 %></p>
                                    <p class="text-[8px] font-black text-slate-600 uppercase tracking-widest">Replies</p>
                                </div>
                                <div class="space-y-1">
                                    <p class="text-white font-bold text-sm"><%= topic.view_count || 0 %></p>
                                    <p class="text-[8px] font-black text-slate-600 uppercase tracking-widest">Views</p>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="text-center py-24 bg-slate-900/20 border border-dashed border-white/5 rounded-[3rem]">
                    <div class="w-16 h-16 bg-slate-800 rounded-full flex items-center justify-center mx-auto mb-6">
                        <i class="fas fa-feather-alt text-2xl text-slate-600"></i>
                    </div>
                    <p class="text-slate-400 font-bold uppercase tracking-widest text-xs">The Great Hall is silent...</p>
                    <button onclick="toggleModal()" class="mt-4 text-blue-500 font-black text-[10px] uppercase underline decoration-2 underline-offset-4">Be the first to speak</button>
                </div>
            <% } %>
        </div>
    </main>

    <div id="topicModal" class="fixed inset-0 z-[100] hidden bg-black/95 backdrop-blur-xl flex items-center justify-center p-4">
        <div class="bg-slate-900 border border-white/10 w-full max-w-xl rounded-[3rem] p-8 lg:p-10 shadow-2xl relative">
            <button onclick="toggleModal()" class="absolute top-8 right-8 text-slate-500 hover:text-white transition-colors">
                <i class="fas fa-times text-xl"></i>
            </button>

            <div class="mb-8">
                <h3 class="text-2xl font-black text-white uppercase italic tracking-tighter">New Decree</h3>
                <p class="text-slate-500 text-xs font-bold uppercase tracking-widest mt-1">Address the village elders</p>
            </div>

            <form action="/forum/create" method="POST" enctype="multipart/form-data" id="forumForm" class="space-y-6">
                <div class="grid grid-cols-2 gap-4">
                    <div class="col-span-2 sm:col-span-1">
                        <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Category</label>
                        <select name="category" id="categorySelect" onchange="togglePriceField()" class="w-full bg-white/5 border border-white/10 rounded-2xl px-5 py-4 text-sm text-white outline-none focus:border-blue-500 transition-all appearance-none cursor-pointer">
                            <option value="general">General Discussion</option>
                            <option value="marketplace">Marketplace</option>
                            <option value="security">Village Security</option>
                            <option value="festivals">Festivals</option>
                        </select>
                    </div>

                    <div id="priceField" class="col-span-2 sm:col-span-1 hidden">
                        <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Price (NGN)</label>
                        <div class="relative">
                            <span class="absolute left-5 top-1/2 -translate-y-1/2 text-slate-500 text-xs font-bold">₦</span>
                            <input type="number" name="price" placeholder="0.00" class="w-full bg-white/5 border border-white/10 rounded-2xl pl-10 pr-5 py-4 text-sm text-white outline-none focus:border-blue-500 transition-all">
                        </div>
                    </div>

                    <div class="col-span-2 sm:col-span-1">
                        <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Upload Media</label>
                        <label class="flex items-center justify-center w-full bg-white/5 border border-dashed border-white/20 rounded-2xl px-5 py-4 cursor-pointer hover:border-blue-500/50 transition-all">
                            <span id="fileNameDisplay" class="text-[10px] text-slate-500 font-bold truncate">Photo/Video</span>
                            <input type="file" name="media" id="forumFile" class="hidden" accept="image/*,video/*">
                        </label>
                    </div>
                </div>

                <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Discussion Subject</label>
                    <input type="text" name="title" placeholder="Give your thread a powerful title..." required class="w-full bg-white/5 border border-white/10 rounded-2xl px-6 py-4 text-sm text-white outline-none focus:border-blue-500 transition-all">
                </div>

                <div>
                    <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2 block">Content</label>
                    <textarea name="content" placeholder="Share your thoughts with the village..." required class="w-full bg-white/5 border border-white/10 rounded-2xl px-6 py-4 text-sm text-white h-40 resize-none outline-none focus:border-blue-500 transition-all"></textarea>
                </div>

                <button type="submit" id="submitBtn" class="w-full bg-blue-600 text-white py-5 rounded-2xl font-black text-[10px] uppercase tracking-[0.3em] hover:bg-blue-500 transition-all shadow-xl shadow-blue-600/20 active:scale-[0.98]">
                    Open Discussion
                </button>
            </form>
        </div>
    </div>

    <script>
        function toggleModal() {
            const modal = document.getElementById('topicModal');
            modal.classList.toggle('hidden');
            document.body.style.overflow = modal.classList.contains('hidden') ? 'auto' : 'hidden';
        }

        // PRICE FIELD TOGGLE
        function togglePriceField() {
            const category = document.getElementById('categorySelect').value;
            const priceField = document.getElementById('priceField');
            if (category === 'marketplace') {
                priceField.classList.remove('hidden');
            } else {
                priceField.classList.add('hidden');
            }
        }

        document.getElementById('forumFile').addEventListener('change', function(e) {
            const fileName = e.target.files[0]?.name || "Upload Media";
            const display = document.getElementById('fileNameDisplay');
            display.innerText = fileName;
            display.classList.add('text-blue-400');
        });

        document.getElementById('forumForm').addEventListener('submit', function() {
            const btn = document.getElementById('submitBtn');
            btn.innerHTML = '<i class="fas fa-spinner animate-spin mr-2"></i> Sealing the Decree...';
            btn.classList.add('opacity-50', 'pointer-events-none');
        });
    </script>
</body>
</html>