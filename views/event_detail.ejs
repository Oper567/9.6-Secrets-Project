<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apugo | Whisper Detail</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #020617; font-family: 'Plus Jakarta Sans', sans-serif; color: #f8fafc; }
        .glass { background: rgba(15, 23, 42, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.05); }
        .custom-scroll::-webkit-scrollbar { width: 4px; }
        .custom-scroll::-webkit-scrollbar-thumb { background: #1e293b; border-radius: 10px; }
    </style>
</head>
<body class="min-h-screen">

    <nav class="fixed top-0 w-full z-50 p-6 flex items-center justify-between">
        <button onclick="history.back()" class="bg-white/5 hover:bg-white/10 text-white w-10 h-10 rounded-full flex items-center justify-center backdrop-blur-md transition">
            <i class="fas fa-chevron-left"></i>
        </button>
        <div class="px-4 py-1 glass rounded-full border border-blue-500/30">
            <span class="text-[10px] font-black uppercase tracking-widest text-blue-400">Village Detail</span>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto h-screen lg:flex lg:pt-0 pt-20">
        
        <div class="lg:w-2/3 h-[50vh] lg:h-screen bg-black/40 flex items-center justify-center p-4">
            <% if (post.image_url) { %>
                <div class="relative w-full h-full flex items-center justify-center">
                    <% if (post.media_type === 'video') { %>
                        <video controls autoplay muted loop class="max-w-full max-h-full rounded-2xl shadow-2xl">
                            <source src="<%= post.image_url %>">
                        </video>
                    <% } else { %>
                        <img src="<%= post.image_url %>" class="max-w-full max-h-full object-contain rounded-2xl shadow-2xl">
                    <% } %>
                </div>
            <% } else { %>
                <div class="text-center p-12 glass rounded-[3rem] border-white/10">
                    <i class="fas fa-quote-left text-4xl text-blue-500/20 mb-4"></i>
                    <p class="text-2xl font-semibold italic text-slate-400">"<%= post.description %>"</p>
                </div>
            <% } %>
        </div>

        <div class="lg:w-1/3 h-screen glass border-l border-white/5 flex flex-col">
            
            <div class="p-8 border-b border-white/5">
                <div class="flex items-center gap-4 mb-6">
                    <img src="<%= post.profile_pic || 'https://via.placeholder.com/100' %>" class="w-14 h-14 rounded-2xl object-cover ring-2 ring-blue-500/20 shadow-xl">
                    <div>
                        <div class="flex items-center gap-2">
                            <h2 class="font-bold text-lg">@<%= post.author.split('@')[0] %></h2>
                            <% if (post.is_verified) { %> <i class="fas fa-check-circle text-blue-500 text-xs"></i> <% } %>
                        </div>
                        <p class="text-[10px] text-slate-500 font-black uppercase tracking-widest">Soul active <%= new Date(post.created_at).toLocaleDateString() %></p>
                    </div>
                </div>
                <p class="text-slate-300 leading-relaxed"><%= post.description %></p>
                
                <div class="flex items-center gap-6 mt-8">
                    <form action="/event/<%= post.id %>/like" method="POST">
                        <button class="flex items-center gap-2 text-[10px] font-black uppercase tracking-widest text-red-500 hover:scale-110 transition">
                            <i class="fas fa-heart text-lg"></i> <%= post.likes_count %> Vibes
                        </button>
                    </form>
                    <div class="text-[10px] font-black uppercase tracking-widest text-slate-500">
                        <i class="fas fa-comment text-lg"></i> <%= post.comments_list ? post.comments_list.length : 0 %> Whispers
                    </div>
                </div>
            </div>

            <div class="flex-grow overflow-y-auto custom-scroll p-8 space-y-6">
                <% if (post.comments_list && post.comments_list.length > 0) { %>
                    <% post.comments_list.forEach(c => { %>
                        <div class="flex gap-4 group">
                            <img src="<%= c.pic || 'https://via.placeholder.com/50' %>" class="w-9 h-9 rounded-xl object-cover opacity-80 group-hover:opacity-100 transition">
                            <div class="flex-grow">
                                <span class="text-[11px] font-bold text-blue-400">@<%= c.author.split('@')[0] %></span>
                                <p class="text-sm text-slate-400 leading-snug mt-1"><%= c.content %></p>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="text-center py-20 opacity-20">
                        <i class="fas fa-wind text-4xl mb-4"></i>
                        <p class="text-xs font-black uppercase tracking-widest">The wind is silent...</p>
                    </div>
                <% } %>
            </div>

            <div class="p-6 bg-slate-900/40 border-t border-white/5">
                <form action="/event/<%= post.id %>/comment" method="POST" class="relative">
                    <input type="text" name="content" placeholder="Whisper something..." required
                        class="w-full bg-slate-950/80 border border-white/10 rounded-2xl py-4 pl-6 pr-16 text-sm outline-none focus:border-blue-500 transition-all text-white placeholder:text-slate-700">
                    <button type="submit" class="absolute right-2 top-2 bottom-2 bg-blue-600 hover:bg-blue-500 text-white px-5 rounded-xl transition shadow-lg shadow-blue-900/20">
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </form>
            </div>
        </div>
    </main>

</body>
</html>