<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discover | Apugo Village</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-[#0b0f1a] text-slate-200 font-sans antialiased">

    <nav class="border-b border-slate-800 bg-[#0b0f1a]/80 backdrop-blur-md sticky top-0 z-50">
        <div class="max-w-5xl mx-auto px-6 py-4 flex justify-between items-center">
            <a href="/feed" class="text-xl font-black tracking-tighter text-blue-500">APUGO <span class="text-slate-200">VILLAGE</span></a>
            <div class="flex gap-6 items-center">
                <a href="/feed" class="text-sm font-bold text-slate-400 hover:text-white transition">Feed</a>
                <a href="/profile" class="text-sm font-bold text-slate-400 hover:text-white transition">Profile</a>
                <a href="/logout" class="text-xs bg-slate-800 px-3 py-1.5 rounded-full font-bold hover:bg-red-500/20 hover:text-red-400 transition">Logout</a>
            </div>
        </div>
    </nav>

    <div class="max-w-5xl mx-auto p-6">
        <header class="mb-10 mt-6">
            <h1 class="text-5xl font-black mb-2 italic tracking-tighter text-white">DISCOVER</h1>
            <div class="flex items-center gap-3">
                <div class="h-1 w-12 bg-blue-600"></div>
                <p class="text-slate-500 text-xs uppercase tracking-[0.3em] font-black">Trending in the village</p>
            </div>
        </header>

        <div class="columns-1 sm:columns-2 lg:columns-3 gap-6 space-y-6">
            <% if (posts && posts.length > 0) { %>
                <% posts.forEach(post => { %>
                    <div class="break-inside-avoid bg-slate-900/50 border border-slate-800 rounded-3xl overflow-hidden hover:border-blue-500/50 hover:bg-slate-900 transition-all duration-500 group cursor-pointer shadow-xl">
                        
                        <% if (post.image_url) { %>
                            <div class="relative overflow-hidden">
                                <% if (post.media_type === 'video') { %>
                                    <video src="<%= post.image_url %>" class="w-full grayscale group-hover:grayscale-0 group-hover:scale-105 transition-all duration-700" muted loop onmouseover="this.play()" onmouseout="this.pause()"></video>
                                    <div class="absolute top-3 right-3 bg-black/50 backdrop-blur-md p-2 rounded-full">
                                        <i class="fas fa-play text-[10px] text-white"></i>
                                    </div>
                                <% } else { %>
                                    <img src="<%= post.image_url %>" class="w-full grayscale group-hover:grayscale-0 group-hover:scale-105 transition-all duration-700" alt="Post">
                                <% } %>
                            </div>
                        <% } %>

                        <div class="p-5">
                            <div class="flex justify-between items-center mb-3">
                                <div class="flex items-center gap-2">
                                    <span class="text-xs font-black text-blue-500 hover:underline">@<%= post.author.split('@')[0] %></span>
                                    <% if (post.is_verified) { %>
                                        <i class="fas fa-check-circle text-[10px] text-blue-400"></i>
                                    <% } %>
                                </div>
                                <span class="text-[10px] font-black text-slate-400 bg-slate-800/50 px-2.5 py-1 rounded-full border border-slate-700/50">
                                    <i class="fas fa-heart text-red-500 mr-1"></i> <%= post.likes || 0 %>
                                </span>
                            </div>
                            <p class="text-sm text-slate-400 group-hover:text-slate-200 transition-colors line-clamp-4 leading-relaxed italic">
                                "<%= post.description %>"
                            </p>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full py-20 text-center border-2 border-dashed border-slate-800 rounded-3xl">
                    <i class="fas fa-mountain text-slate-700 text-4xl mb-4"></i>
                    <p class="text-slate-500 font-bold">The village is quiet today...</p>
                    <a href="/feed" class="text-blue-500 text-sm hover:underline mt-2 inline-block">Go back and post something!</a>
                </div>
            <% } %>
        </div>
    </div>

    <div class="h-20"></div>

</body>
</html>